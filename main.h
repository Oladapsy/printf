#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdlib.h>
#include <unistd.h>
#include <stdarg.h>

int _putint(int in);
int _printf(const char *format, ...);
int my_putchar(char ch);
int dee_puts(char *st);
#endif
